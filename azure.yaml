# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: protect-apim-with-oauth
metadata:
  template: tdd-protect-apim-with-oauth@1.7.1
hooks:
  postprovision:
    - shell: pwsh
      run: ./hooks/postprovision-create-and-store-client-secrets.ps1
      interactive: true
  predown:
    - shell: pwsh
      run: ./hooks/predown-remove-app-registrations.ps1
      interactive: true
requiredVersions:
  azd: ">= 1.23.15" # azd version 1.23.15 or higher is required because we're using features from Bicep 0.42.1.
